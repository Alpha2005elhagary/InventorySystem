@model InventorySystem.ViewModels.StockTransactionViewModel

@{
    ViewData["Title"] = "Record Stock Out";
}

<div class="container py-2" style="max-width: 600px;">
    <div class="mb-4">
        <a asp-action="Index" class="text-decoration-none text-muted small d-inline-flex align-items-center gap-1 mb-2">
            <i class="ti ti-arrow-left"></i> Back to Ledger
        </a>
        <h3 class="fw-bold text-danger mb-1">
            <i class="ti ti-arrow-bar-to-up me-1"></i> Record Stock Out (Dispatch / Sale)
        </h3>
        <p class="text-muted small">Dispatch goods for sale or transfer, decreasing the available inventory count.</p>
    </div>

    <div class="card border-0 shadow-sm">
        <div class="card-body p-4">
            <form asp-action="StockOut" method="post">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="alert alert-danger border-0 small py-2"></div>

                <div class="mb-3">
                    <label asp-for="ProductId" class="form-label fw-semibold small">Select Product <span class="text-danger">*</span></label>
                    <select asp-for="ProductId" asp-items="Model.Products" class="form-select" required>
                        <option value="">-- Choose Item --</option>
                    </select>
                    <span asp-validation-for="ProductId" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Quantity" class="form-label fw-semibold small">Quantity Dispatched <span class="text-danger">*</span></label>
                    <input asp-for="Quantity" type="number" min="1" class="form-control" placeholder="e.g. 5" required />
                    <span asp-validation-for="Quantity" class="text-danger small"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="Notes" class="form-label fw-semibold small">Sale Reference / Reason</label>
                    <textarea asp-for="Notes" class="form-control" rows="3" placeholder="e.g. Order #2041 - Dispatched to retail showroom"></textarea>
                    <span asp-validation-for="Notes" class="text-danger small"></span>
                </div>

                <div class="d-flex justify-content-end gap-2">
                    <a asp-action="Index" class="btn btn-light px-4">Cancel</a>
                    <button type="submit" class="btn btn-danger px-4 d-flex align-items-center gap-2">
                        <i class="ti ti-minus"></i> Deduct from Stock
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
